draw_config:
  layout:
    ortho_layout:
      split: true
      rows: 4
      columns: 6
      thumbs: 5

  parse_config:
    preprocess: true
    zmk_keycode_map:
      BACKSPACE: "Bksp"
      ESCAPE: "Esc"
